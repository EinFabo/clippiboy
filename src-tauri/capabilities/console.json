{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "console",
  "description": "Die Konsole über dem Spiel — sie wird bedient, anders als der Banner",
  "windows": ["console"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
